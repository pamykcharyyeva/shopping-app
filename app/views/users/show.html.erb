<div class="ui clearing segment">
  <h2 class="ui left floated header">

    <h2 class="ui teal header"> <%= current_user.username %>'s Show Page</h2>
    <%= button_to 'Logout', logout_path, method: :delete, class: 'ui grey button' %>

  </h2>
</div>

<h4 class="ui horizontal divider header">
  <i class="tag icon"></i>
  Controls
</h4>
<p><center>Options below to create and view your favorite websites</center></p>
<p><h3><%= link_to "Create New Favorite", new_favorite_path(@user.id) %></h3></p>

<!-- <p><#% @user.favorites.each do |favorite| %></p> -->
    <p><h3><%= link_to "Favorite Websites", show_favorite_path(@user.id) %></h3></p>



<% if current_user == @user %>
  <%= button_to 'Delete This Account', user_path(@user), method: :delete, data: { confirm: 'Are you sure you want to delete this account?' }, class: 'ui red button' %>
<% end %>
