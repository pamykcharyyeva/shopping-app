<h1> USER SHOW PAGE</h1>
<p><%= @user.username %></p>
<%= button_to 'Logout', logout_path, method: :delete %>


<p><%= link_to "Create new favorite", new_favorite_path(@user.id) %></p>

<!-- <p><#% @user.favorites.each do |favorite| %></p> -->
    <p><%= link_to "Favorite Websites", show_favorite_path(@user.id) %></p>



<% if current_user == @user %>
  <%= button_to 'Delete This Account', user_path(@user), method: :delete, data: { confirm: 'Are you sure you want to delete this account?' } %>
<% end %>
